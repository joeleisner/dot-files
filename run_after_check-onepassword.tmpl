#!/usr/bin/env bash

set -e

if ! command -v "{{ includeTemplate "onepassword-cmd" . }}" &> /dev/null; then
	echo "⚠️  1Password CLI ({{ includeTemplate "onepassword-cmd" . }}) not found in PATH. Git commit signing and 1Password-sourced SSH config were skipped this apply. Once it's installed and configured, run: chezmoi apply" >&2
fi

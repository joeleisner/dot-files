#!/usr/bin/env bash

if [ -z "$1" ]
then
	echo "domain name missing"
	exit 1
fi

name="$1"
information=$(echo | openssl s_client -connect "$name:443" 2>/dev/null | openssl x509 -noout -enddate -serial -subject -ext subjectAltName)

echo -e "$information";
